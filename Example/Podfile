use_frameworks!

source 'git@github.com:VivyTeam/specs-ios.git'
source 'https://cdn.jsdelivr.net/cocoa/'

target 'Krypt_Example' do
  pod 'Krypt', :path => '../'

  target 'Krypt_Tests' do
    inherit! :search_paths

    
  end
end
